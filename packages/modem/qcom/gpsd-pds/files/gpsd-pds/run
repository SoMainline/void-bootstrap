#!/bin/sh
[ -r conf ] && . ./conf
if grep -q 'qcom,' /proc/device-tree/compatible 2>/dev/null && qrtr-lookup | grep -q 'PDS'; then
	: ${DEV:=pds://any}
else
	: ${DEV:=/dev/gps0}
fi
exec /usr/bin/gpsd -N -F /run/gpsd.sock $OPTS $DEV
